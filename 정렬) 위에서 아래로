# 위에서 아래로
  하나의 수얼에는 다잉한 수가 존재한다. 이러한	수는 크기에 싱관없이 나열되어 있다.	
  이	수를 큰 수부터 작은 수의 순서로	정렬해야	한다.	수열을	내림차순으로 정렬하는 프로그램을 만드시오
  
  첫째줄에 수열에 속해있는 수의 개수 N이 주어진다.(1≤N≤500)
  둘째줄부터 N+1번째 줄까지 N개의 수가 입력된다. 수의 범위는 1이상 1OO,OOO 이하의 자연수이다. 
  

n = int(input())   # N을 입력받기

array = []       # N개의 정수를 입력받아	리스트에 저장
for i in range(n) : 
    array.append(int(input())
    
array = sorted(array, reverse = True)     #	파이썬	정렬 라이브러리를 이용하여 정렬 수행   # 내림차순

for i in array:    # 정렬이	수행된 결과를 출력
    print(i, end='')
    
