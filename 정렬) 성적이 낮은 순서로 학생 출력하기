n = int(input())

array =[]
for i in range(n):
    input_data = input.split()         
    # 이름을 문자열 그대로, 점수는 정수형으로 변환 후 저장.
    array.append((input_data[0], int(input_data[1])))
    
    # 키(key)를 이용하여, 점수를 기준으로 정렬     # 람다(lambda)란 단 한 번만 사용할 간단한 함수의 선언과 호출을 하나의 식으로 간략히 표현한 것
    # 리스트 함수인 sort() 함수는 원본 리스트 그 자체를 정렬하여 변경하지만, 파이썬 내장 함수인 sorted() 함수는 원본 리스트는 변경하지 않고 정렬된 새로운 리스트를 반환해 줍니다.
    array = sorted(array, key = lambda student : students[1]) 
    
    # 정렬이 수행된 결과를 출력
    for student in array :
        print(student[0], end=' ')
    
